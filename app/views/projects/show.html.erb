
<h4 align="center"> <%= @proj.name.capitalize %>  </h4>



    <div class="row">
    
        <div class="col-md-8 col-md-offset-2 ">
            <h4> Name : <%=  @proj.name.capitalize %></h4>
            <p>  Description : <%= truncate(@proj.description, length:100) %> </p>
           
            <br>
            <P>
            <%= link_to " All Projects " , projects_path,class: "btn btn-xs btn-warning" %>
            <% if can? :update, @proj %>
                 <%= link_to "Edit Project", edit_project_path(@proj), class: "btn btn-xs btn-warning" %>
            <% end %>
            <% if can? :update, @proj %>
                <%= link_to "Delete Project", project_path(@proj), method: :delete, 
                                                data: {confirm:"are you sure about deleting this project"},
                                                class: "btn btn-xs btn-danger" %></p>
            <% end %>
        </div>
    </div>
     
